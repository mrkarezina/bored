---
description: Open the default example game
allowed-tools:
  - Bash
  - Read
  - Write
model: haiku
---

# /bored-default — Open the Example Game

Open the built-in "Neon Drift" demo game instantly.

## Steps

1. Read `examples/sample-game.html` from the bored project directory
2. Write it to `./index.html` in the current working directory
3. Run `open index.html` to launch it in the browser
4. Tell the user: "Opened the **Neon Drift** demo game. Use `/bored` to generate a unique game with a custom theme!"
